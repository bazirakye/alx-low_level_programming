#include "main.h"
#include <stdio.h>

/**
 * main - Main function
 * @argc: argument counter
 * @argv: argument vector array
 * Return: return 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
